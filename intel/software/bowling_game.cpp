need pins, and a total score

int total_score
unorded_map<int, int> pins, first is the pin number and the second is the pin score

each time will return a vector<int> pins_hit
and get the score of each hit